%p#notice= notice
%h1 Listing Regions
%table.table.table-striped
  %thead
    %tr
      %th Изменить
      %th Название
  %tbody
    - @regions.each do |region|
      %tr
        %td= link_to 'Edit', edit_region_path(region), class: 'btn btn-success'
        %td= link_to region.name, region
        -#%td= link_to 'Destroy', region, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Region', new_region_path